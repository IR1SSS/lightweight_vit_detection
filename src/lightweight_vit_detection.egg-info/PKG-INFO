Metadata-Version: 2.4
Name: lightweight_vit_detection
Version: 1.0.0
Summary: Lightweight Vision Transformer for Real-time Object Detection
Home-page: https://github.com/example/lightweight-vit-detection
Author: CDS540 Project Team
Author-email: team@example.com
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: Intended Audience :: Science/Research
Classifier: License :: OSI Approved :: MIT License
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Topic :: Scientific/Engineering :: Artificial Intelligence
Classifier: Topic :: Scientific/Engineering :: Image Recognition
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: torch>=1.12.0
Requires-Dist: torchvision>=0.13.0
Requires-Dist: torchaudio>=0.12.0
Requires-Dist: opencv-python>=4.5.0
Requires-Dist: Pillow>=9.0.0
Requires-Dist: albumentations>=1.3.0
Requires-Dist: onnx>=1.12.0
Requires-Dist: onnxruntime>=1.12.0
Requires-Dist: onnxruntime-gpu>=1.12.0
Requires-Dist: numpy>=1.21.0
Requires-Dist: scipy>=1.7.0
Requires-Dist: pycocotools>=2.0.4
Requires-Dist: pyyaml>=6.0
Requires-Dist: omegaconf>=2.2.0
Requires-Dist: tensorboard>=2.10.0
Requires-Dist: tqdm>=4.64.0
Requires-Dist: colorlog>=6.6.0
Requires-Dist: pytest>=7.0.0
Requires-Dist: pytest-cov>=3.0.0
Requires-Dist: packaging>=21.0
Provides-Extra: tensorrt
Requires-Dist: nvidia-tensorrt>=8.0.0; extra == "tensorrt"
Provides-Extra: dev
Requires-Dist: black>=22.0.0; extra == "dev"
Requires-Dist: isort>=5.10.0; extra == "dev"
Requires-Dist: flake8>=4.0.0; extra == "dev"
Requires-Dist: mypy>=0.950; extra == "dev"
Dynamic: author
Dynamic: author-email
Dynamic: classifier
Dynamic: description
Dynamic: description-content-type
Dynamic: home-page
Dynamic: provides-extra
Dynamic: requires-dist
Dynamic: requires-python
Dynamic: summary

# 轻量化视觉Transformer实时物体检测系统

[![Python 3.8+](https://img.shields.io/badge/Python-3.8%2B-blue.svg)](https://www.python.org/downloads/)
[![PyTorch 1.12+](https://img.shields.io/badge/PyTorch-1.12%2B-orange.svg)](https://pytorch.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

基于轻量化视觉Transformer的实时物体检测系统，在保持高精度的同时实现 >30 FPS 的实时性能，适用于移动设备和边缘计算场景。

## 特性

- **轻量化骨干网络**: 支持 MobileViT-Small/Base、EfficientFormer 等轻量化架构
- **高效注意力机制**: 线性注意力、池化注意力，复杂度从 O(n²) 降至 O(n)
- **知识蒸馏框架**: 支持响应蒸馏、特征蒸馏、关系蒸馏等多种策略
- **模型优化**: 量化感知训练(QAT)、结构化剪枝、ONNX/TensorRT 导出
- **实时推理**: 支持图像、视频流实时检测，可视化结果

## 项目结构

```
Light Weight ViT/
├── configs/                    # 配置文件
│   ├── model/                  # 模型配置
│   ├── train/                  # 训练配置
│   └── deploy/                 # 部署配置
├── src/                        # 源代码
│   ├── models/                 # 模型定义
│   │   ├── backbone/           # 骨干网络
│   │   ├── neck/               # 特征融合
│   │   └── head/               # 检测头
│   ├── distillation/           # 知识蒸馏
│   ├── optimization/           # 模型优化
│   ├── inference/              # 推理引擎
│   ├── data/                   # 数据处理
│   └── utils/                  # 工具函数
├── tools/                      # 工具脚本
│   ├── train.py                # 训练入口
│   ├── evaluate.py             # 模型评估
│   ├── export_model.py         # 模型导出
│   └── demo.py                 # 演示脚本
└── tests/                      # 单元测试
```

## 安装

### 环境要求

- Python 3.8+
- PyTorch 1.12+
- CUDA 11.0+ (GPU 推理)

### 安装步骤

```bash
# 克隆项目
git clone https://github.com/your-repo/lightweight-vit-detection.git
cd lightweight-vit-detection

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/Mac
# or
.\venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 安装项目
pip install -e .
```

## 快速开始

### 1. 准备数据集

下载 COCO 2017 数据集并解压到 `data/coco/` 目录：

#### Linux/Mac

```bash
# 创建数据目录
mkdir -p data/coco && cd data/coco

# 下载训练集 (约 18GB)
wget http://images.cocodataset.org/zips/train2017.zip

# 下载验证集 (约 1GB)
wget http://images.cocodataset.org/zips/val2017.zip

# 下载标注文件 (约 241MB)
wget http://images.cocodataset.org/annotations/annotations_trainval2017.zip

# 解压
unzip train2017.zip
unzip val2017.zip
unzip annotations_trainval2017.zip

# 清理压缩包
rm *.zip
```

#### Windows (PowerShell)

```powershell
# 创建数据目录
New-Item -ItemType Directory -Force -Path data\coco
cd data\coco

# 下载训练集
Invoke-WebRequest -Uri http://images.cocodataset.org/zips/train2017.zip -OutFile train2017.zip

# 下载验证集
Invoke-WebRequest -Uri http://images.cocodataset.org/zips/val2017.zip -OutFile val2017.zip

# 下载标注文件
Invoke-WebRequest -Uri http://images.cocodataset.org/annotations/annotations_trainval2017.zip -OutFile annotations_trainval2017.zip

# 解压 (需要 7-Zip 或系统自带解压)
Expand-Archive train2017.zip -DestinationPath .
Expand-Archive val2017.zip -DestinationPath .
Expand-Archive annotations_trainval2017.zip -DestinationPath .

# 清理压缩包
Remove-Item *.zip
```

#### 使用脚本快速下载

```bash
# 使用项目提供的脚本下载
python scripts/download_coco.py --output-dir data/coco
```

数据目录结构：

```
data/coco/
├── train2017/
├── val2017/
└── annotations/
    ├── instances_train2017.json
    └── instances_val2017.json
```

### 2. 训练模型

```bash
# 使用知识蒸馏训练
python tools/train.py --config configs/train/distillation.yaml

# 恢复训练
python tools/train.py --config configs/train/distillation.yaml --resume outputs/checkpoint.pth
```

### 3. 评估模型

```bash
python tools/evaluate.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --data-root data/coco/val2017 \
    --annotation data/coco/annotations/instances_val2017.json
```

### 4. 导出模型

```bash
# 导出 ONNX
python tools/export_model.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --format onnx \
    --simplify

# 导出 TorchScript
python tools/export_model.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --format torchscript

# 导出 TensorRT (需要安装 TensorRT)
python tools/export_model.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --format tensorrt \
    --tensorrt-precision fp16
```

### 5. 运行演示

```bash
# 图像检测
python tools/demo.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --source path/to/image.jpg \
    --show

# 视频检测
python tools/demo.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --source path/to/video.mp4 \
    --output output.mp4

# 实时摄像头检测
python tools/demo.py \
    --config configs/model/mobilevit_small.yaml \
    --weights outputs/best_model.pth \
    --source 0
```

## 模型配置

### MobileViT-Small

轻量级模型，适合移动端部署：

| 指标 | 目标值 |
|------|--------|
| 参数量 | ~5.6M |
| 模型大小 | ~22 MB |
| mAP@0.5 | > 0.75 |
| FPS (RTX 3060) | > 30 |

### EfficientFormer-L1

更高精度，适合边缘设备：

| 指标 | 目标值 |
|------|--------|
| 参数量 | ~12.3M |
| 模型大小 | ~48 MB |
| mAP@0.5 | > 0.77 |
| FPS (RTX 3060) | > 35 |

## 知识蒸馏

支持三种蒸馏策略：

### 响应蒸馏 (Response Distillation)

```yaml
distillation:
  strategies:
    response:
      enabled: true
      weight: 1.0
      temperature: 4.0
```

### 特征蒸馏 (Feature Distillation)

```yaml
distillation:
  strategies:
    feature:
      enabled: true
      weight: 0.5
      layers: [2, 3, 4]
      loss_type: "mse"
```

### 关系蒸馏 (Relation Distillation)

```yaml
distillation:
  strategies:
    relation:
      enabled: true
      weight: 0.3
      loss_type: "spatial_attention"
```

## 模型优化

### 量化感知训练

```python
from src.optimization import QuantizationAwareTraining

qat = QuantizationAwareTraining(model)
prepared_model = qat.prepare()
# 训练...
quantized_model = qat.convert()
```

### 模型剪枝

```python
from src.optimization import L1StructuredPruner

pruner = L1StructuredPruner(model, pruning_ratio=0.3)
pruned_model = pruner.prune()
```

## 性能基准

与主流检测器对比：

| 模型 | FPS | 大小 | mAP@0.5 |
|------|-----|------|---------|
| YOLOv5s | 140 | 28 MB | 0.68 |
| SSD-MobileNetV2 | 60 | 17 MB | 0.62 |
| EfficientDet-D0 | 55 | 15 MB | 0.67 |
| **MobileViT-S (Ours)** | **30+** | **<50 MB** | **>0.75** |

## 开发指南

### 运行测试

```bash
# 运行所有测试
pytest tests/ -v

# 运行特定测试
pytest tests/test_models.py -v

# 测试覆盖率
pytest tests/ --cov=src --cov-report=html
```

### 代码风格

```bash
# 格式化代码
black src/ tools/ tests/

# 检查代码风格
flake8 src/ tools/ tests/

# 类型检查
mypy src/
```

## 参考文献

1. MobileViT: Light-weight, General-purpose, and Mobile-friendly Vision Transformer
2. EfficientFormer: Vision Transformers at MobileNet Speed
3. Knowledge Distillation: A Survey
4. COCO: Common Objects in Context Dataset

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件。

## 致谢

- [PyTorch](https://pytorch.org/)
- [MMDetection](https://github.com/open-mmlab/mmdetection)
- [ONNX Runtime](https://onnxruntime.ai/)
- [TensorRT](https://developer.nvidia.com/tensorrt)
